import { FormatNumberInputDirective } from './format-number-input.directive';

describe('FormatNumberInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatNumberInputDirective();
    expect(directive).toBeTruthy();
  });
});
