import { PhoneNumberDirectiveDirective } from './phone-number-directive.directive';

describe('PhoneNumberDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNumberDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
